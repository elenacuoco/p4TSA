
language: python
python:
  - "3.6"
cache: pip
install:
  - pip install -r requirements.txt 
script:
  - pip install -r requirements.txt
  - cd python-wrapper
  - python setup.py install  
    # command to run tests
  - python setup.py test
   #
  - cd pyWDF
  - python setup.py install  

